language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-refluxify' ]; then cd .. && eval "mv $currentfolder generator-refluxify" && cd generator-refluxify; fi
